name: 'dw_lab1'
version: '1.0.0'
config-version: 2
profile: 'dw_lab1'
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
clean-targets:
  - "target"
  - "dbt_packages"
models:
  dw_lab1:
    +materialized: table
